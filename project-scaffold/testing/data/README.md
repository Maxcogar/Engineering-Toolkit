# Test Data

Raw, unprocessed test data.

## Guidelines

- Never modify raw data files
- Include metadata (date, conditions, equipment)
- Use consistent naming: `YYYY-MM-DD_test-name_raw.ext`

## Supported Formats

- CSV for tabular data
- JSON for structured data
- Images/video for visual tests
